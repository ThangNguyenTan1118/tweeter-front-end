mutation SignIn($password: String!, $email: String!) {
  signin(password: $password, email: $email) {
    user {
      id
      username
      email
      createdAt
      updatedAt
    }
    token
  }
}
